# SQL Migrations

This directory contains all the raw SQL migration scripts for the Supabase database schema. Each file represents a change to the database structure, such as creating tables, adding columns, or defining policies.