# Custom Hooks

This directory contains custom React hooks to encapsulate and reuse stateful logic across components.

- `useAuth.ts`: Manages user authentication state.
- `usePosts.ts`: Fetches and manages social media posts.
- `useWallet.ts`: Manages user wallet/credit balance.
- `use-mobile.tsx`: Detects if the user is on a mobile device.